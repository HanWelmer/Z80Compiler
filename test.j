/*
 * A small program in the miniJava language.
 * Test something
 */
class TestWrite {
  word zero = 0;
  byte one = 1;
  write(4);
  write("Hallo wereld");
  String str = "Hello too.";
  write(str);
  write(one);
  write(zero);
}
