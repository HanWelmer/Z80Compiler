/*
 * A small program in the miniJava language.
 * Test something
 */
class Test {
  int zero = 0;
  int four = 4;
  byte byteOne = 1;
  if (four == 0 + 12/(1 + 2)) write(2);
  write(byteOne);
  write(zero);
}
