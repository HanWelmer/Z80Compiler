/*
 * A small program in the miniJava language.
 * Test comparisons
 */
class TestIf {
  int zero = 0;
  int one = 1;
  int four = 4;
  int twelve = 12;
  write(3);
  if (4 == (zero + twelve/(1+2))) write(2);
  if (four == (0 + 12/(one+2))) write(1);
  write(0);
}
